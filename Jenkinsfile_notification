pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
    post {
        always {
            emailext to: "pedrojok@gmail.com",
            subject: "${env.JOB_NAME} na build [${env.BUILD_NUMBER}]",
            body: "Verifique a saída da console do Job ${env.JOB_NAME} em [${env.BUILD_URL}] "
        }   
    }
}
